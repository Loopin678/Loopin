export type User ={
    id: string;
    name: string;
    email: string;
    passwordHash: string;
    createdAt: Date;
};

/*
This representation is temporary so far and this will be updated with the confirmed prisma schema
*/